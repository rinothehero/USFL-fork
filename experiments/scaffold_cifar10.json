{
  "method": "scaffold",
  "dataset": "cifar10",
  "_dataset_options": "cifar10 | cifar100 | fmnist | mnist | svhn",
  "model": "resnet18",
  "_model_options": "resnet18 | vgg11 | alexnet | mobilenet | lenet | deit_tiny | mlp",
  "split_layer": "layer2",
  "rounds": 100,
  "num_clients": 100,
  "clients_per_round": 10,
  "local_epochs": 5,
  "batch_size": 50,
  "learning_rate": 0.001,
  "momentum": 0.0,
  "weight_decay": 0.0,
  "optimizer": "sgd",
  "_optimizer_options": "sgd | adam | adamw",
  "distribution": "shard_dirichlet",
  "_distribution_options": "uniform | dirichlet | shard_dirichlet",
  "alpha": 0.3,
  "labels_per_client": 2,
  "selector": "uniform",
  "_selector_options": "uniform",
  "aggregator": "fedavg",
  "_aggregator_options": "fedavg",
  "exhaustion_policy": "cycling",
  "_exhaustion_policy_options": "cycling | skip | break",
  "device": "cuda",
  "_device_options": "cuda | cpu",
  "seed": 42,

  "_note": "SCAFFOLD uses per_client server training mode (same as SFL). Control variates correct gradient drift in Non-IID."
}
